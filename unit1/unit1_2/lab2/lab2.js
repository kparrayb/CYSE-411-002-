// 1. Returns true only if value is the number 0
function isZero(value) {}

// 2. Returns true only if input is a non-empty string
function isValidUsername(input) {}

// 3. Returns true only if flag is boolean true
function isTrueFlag(flag) {}